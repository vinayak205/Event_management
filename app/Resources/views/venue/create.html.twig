{% extends 'base.html.twig' %}
{% block body %}
	<h2 class="page-header">Add Venue</h2>
	{{form_start(form)}}
	{{form_widget(form)}}
	<input type="submit" value="Save" />
	{{form_end(form)}}
{% endblock %}
